import './header.css';
import { Navbar } from '../../components';

const Header = () => {
  return (
    <section className='bg-gradient-primary'>
      <Navbar />
    </section>
  );
};

export default Header;
